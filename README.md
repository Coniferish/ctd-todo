# Todo App
This is a todo app that will be built with React and Vite as the UI Library and Framework.

## Installation
Clone this repo.
`cd` into the project.
Run `npm install`.
Run `npm run dev` to run the app locally. The logs will provide a url on localhost where you can see the working app.